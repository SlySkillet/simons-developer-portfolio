import React from 'react';

function ProjectCard({
    projectTitle,
    completedDate,
    imageUrls,
    techStack,
    description,
    liveLink,
    repoLink
}) {
    return(<p>returning</p>)
}

export default ProjectCard
